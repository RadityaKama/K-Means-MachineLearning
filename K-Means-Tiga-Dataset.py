# -*- coding: utf-8 -*-
"""5240411098_RadityaKamaCahyadewa_K-Means.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1ObzFiMlgPTve8ZK9CaKET_Z1kSVjBjJq

K-Means for dataset Mall_Customers

Analisis Segmentasi Pelanggan Pusat Perbelanjaan Menggunakan Algoritma K-Means Clustering

Sebagai representasi manajemen pusat perbelanjaan atau supermarket, memahami karakteristik pelanggan adalah fondasi utama dalam merancang strategi bisnis yang presisi. Dataset ini mendokumentasikan informasi dasar pelanggan yang dihimpun secara sistematis melalui program kartu keanggotaan. Variabel yang tercakup meliputi identitas pelanggan, usia, jenis kelamin, pendapatan tahunan, serta skor pengeluaran. Skor pengeluaran itu sendiri merupakan sebuah metrik khusus yang ditetapkan oleh pihak manajemen berdasarkan parameter perilaku konsumtif dan riwayat data pembelian masing-masing pelanggan.

Fokus utama dari rumusan masalah pada analisis ini adalah kebutuhan untuk memahami profil pelanggan secara mendalam guna mengidentifikasi kelompok target utama, yaitu mereka yang paling potensial dan mudah diarahkan untuk melakukan transaksi. Melalui pendekatan pembelajaran mesin tanpa pengawasan (unsupervised machine learning), khususnya algoritma K-Means Clustering, kita dapat melakukan pemetaan segmentasi pasar. Hasil dari pengelompokan ini nantinya akan menjadi landasan faktual bagi tim pemasaran dalam menyusun strategi promosi yang lebih terarah, efisien, dan tepat sasaran.

Untuk memastikan keakuratan dalam menentukan jumlah kelompok (K) yang paling representatif, analisis ini menggunakan dua metode evaluasi utama:

1. Metode Elbow (WCSS): Metode ini mengevaluasi nilai jarak kuadrat antara setiap titik data dengan pusat cluster di dalam kelompok yang sama (Within-Cluster Sum of Squares). Tujuannya adalah meminimalkan variansi internal cluster. Penentuan jumlah cluster yang optimal dilihat pada titik di mana kurva penurunan grafik mulai melandai secara signifikan dan membentuk pola menyerupai siku. Titik tersebut menandakan bahwa penambahan cluster baru tidak lagi memberikan reduksi variansi yang bermakna.

2. Metode Silhouette: Sebagai pendekatan komplementer, metode ini digunakan untuk mengukur seberapa baik tingkat pemisahan antar cluster. Metrik Silhouette mengevaluasi tingkat kohesi (kedekatan suatu data dengan kelompoknya sendiri) sekaligus tingkat separasi (jarak data tersebut terhadap kelompok yang berbeda). Nilai yang dihasilkan berada pada rentang angka negatif satu hingga positif satu. Titik puncak tertinggi pada grafik metrik ini merepresentasikan konfigurasi jumlah cluster yang paling ideal dan terpisah dengan jelas.
"""

import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns
from sklearn.cluster import KMeans
from sklearn.metrics import silhouette_score

df = pd.read_csv('Mall_Customers.csv')
X = df.iloc[:, [3, 4]].values

wcss = []
for i in range(1, 11):
    kmeans = KMeans(n_clusters=i, init='k-means++', random_state=42, n_init=10)
    kmeans.fit(X)
    wcss.append(kmeans.inertia_)

plt.figure(figsize=(10, 5))
plt.plot(range(1, 11), wcss, marker='o')
plt.title('Metode Elbow (WCSS)')
plt.xlabel('Jumlah Cluster (K)')
plt.ylabel('WCSS')
plt.grid(True)
plt.show()

silhouette_scores = []
for i in range(2, 11):
    kmeans = KMeans(n_clusters=i, init='k-means++', random_state=42, n_init=10)
    labels = kmeans.fit_predict(X)
    score = silhouette_score(X, labels)
    silhouette_scores.append(score)

plt.figure(figsize=(10, 5))
plt.plot(range(2, 11), silhouette_scores, marker='o')
plt.title('Metode Silhouette')
plt.xlabel('Jumlah Cluster (K)')
plt.ylabel('Silhouette Score')
plt.grid(True)
plt.show()

for k in range(1, 6):
    kmeans = KMeans(n_clusters=k, init='k-means++', random_state=42, n_init=10)
    y_kmeans = kmeans.fit_predict(X)

    plt.figure(figsize=(8, 5))
    if k == 1:
        sns.scatterplot(x=X[:, 0], y=X[:, 1], color='dodgerblue', s=100)
    else:
        sns.scatterplot(x=X[:, 0], y=X[:, 1], hue=y_kmeans, palette='viridis', s=100)

    plt.scatter(kmeans.cluster_centers_[:, 0], kmeans.cluster_centers_[:, 1], s=300, c='red', label='Centroids', marker='X')
    plt.title(f'Hasil Clustering (K={k})')
    plt.xlabel('Annual Income (k$)')
    plt.ylabel('Spending Score (1-100)')
    plt.legend()
    plt.show()

"""K-Means for dataset bank_transactions_data_2

Analisis Segmentasi Transaksi Perbankan Menggunakan Algoritma K-Means Clustering

Keamanan finansial dan pemantauan aktivitas rekening merupakan aspek krusial dalam operasional lembaga keuangan saat ini. Dataset yang dianalisis ini menyajikan rekam jejak yang sangat mendetail mengenai perilaku transaksional dan pola aktivitas finansial melalui 2.512 sampel data. Di dalamnya terdapat cakupan informasi yang komprehensif terkait atribut transaksi, demografi nasabah, hingga metrik pola penggunaan. Keberagaman fitur yang tersedia, mulai dari nominal transaksi, metrik waktu, saluran transaksi, hingga profil nasabah seperti usia dan pekerjaan, menjadikan kumpulan data ini instrumen yang ideal untuk dieksplorasi oleh ilmuwan data dan analis keuangan. Eksplorasi ini secara khusus ditujukan untuk membangun model prediktif yang berfokus pada deteksi penipuan (fraud detection) dan identifikasi anomali transaksi.

Dalam implementasinya, algoritma K-Means Clustering difungsikan untuk memetakan perilaku nasabah ke dalam beberapa klaster berdasarkan tingkat kemiripan atribut transaksinya. Pada analisis ini, variabel utama yang difokuskan adalah nominal transaksi (Transaction Amount) dan saldo rekening pasca-transaksi (Account Balance). Pengelompokan berdasarkan kedua dimensi finansial ini memungkinkan sistem untuk membedakan antara pengeluaran wajar harian dengan transaksi berskala besar yang mungkin mencurigakan. Wawasan menyeluruh dari hasil segmentasi ini sangat esensial untuk memisahkan pola normal dari aktivitas yang menyimpang. Transaksi yang masuk ke dalam klaster minoritas atau berjarak sangat jauh dari pusat kebiasaan umum dapat diinvestigasi lebih lanjut sebagai potensi anomali atau percobaan akses yang tidak sah. Walaupun dataset ini dirancang khusus untuk tugas pengenalan pola pembelajaran mesin dan bukan sebagai sumber data primer untuk publikasi akademik, representasinya sangat relevan untuk mensimulasikan lingkungan keamanan finansial dunia nyata.

Sama halnya dengan pendekatan analitik terstruktur lainnya, penentuan jumlah klaster optimal dalam pemetaan transaksi perbankan ini bersandar pada dua metrik evaluasi utama:
1. Metode Elbow (WCSS): Pendekatan ini berfokus pada pengukuran total variansi internal untuk memastikan bahwa setiap titik transaksi di dalam satu klaster memiliki tingkat kemiripan yang sangat tinggi. Metrik yang dihitung adalah jarak kuadrat antara setiap data dengan pusat klasternya. Titik optimal diidentifikasi pada bagian kurva grafik yang mengalami penurunan drastis kemudian mulai melandai secara tajam membentuk pola menyerupai sudut siku. Pola siku tersebut secara matematis mengindikasikan bahwa pembagian kelompok telah mencapai titik kestabilan yang efisien, di mana penambahan klaster baru tidak lagi memberikan manfaat reduksi variansi yang berarti.

2. Metode Silhouette: Evaluasi ini mengukur tingkat kualitas dan ketegasan batas pemisahan antar klaster transaksi. Dengan menghitung rentang nilai kelayakan yang berkisar dari angka negatif satu hingga positif satu, metrik ini akan memvalidasi apakah suatu titik transaksi benar-benar cocok berada di klasternya sendiri sekaligus memiliki jarak separasi yang cukup jauh dari klaster lainnya. Puncak tertinggi pada grafik skor ini akan menjadi landasan final yang paling objektif dalam menentukan jumlah klaster segmentasi yang terdefinisi dengan sangat baik dan terhindar dari tumpang tindih data.
"""

import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns
from sklearn.cluster import KMeans
from sklearn.metrics import silhouette_score

df = pd.read_csv('bank_transactions_data_2.csv')
X = df[['TransactionAmount', 'AccountBalance']].values

wcss = []
for i in range(1, 11):
    kmeans = KMeans(n_clusters=i, init='k-means++', random_state=42, n_init=10)
    kmeans.fit(X)
    wcss.append(kmeans.inertia_)

plt.figure(figsize=(10, 5))
plt.plot(range(1, 11), wcss, marker='o')
plt.title('Metode Elbow (WCSS)')
plt.xlabel('Jumlah Cluster (K)')
plt.ylabel('WCSS')
plt.grid(True)
plt.show()

silhouette_scores = []
for i in range(2, 11):
    kmeans = KMeans(n_clusters=i, init='k-means++', random_state=42, n_init=10)
    labels = kmeans.fit_predict(X)
    score = silhouette_score(X, labels)
    silhouette_scores.append(score)

plt.figure(figsize=(10, 5))
plt.plot(range(2, 11), silhouette_scores, marker='o')
plt.title('Metode Silhouette')
plt.xlabel('Jumlah Cluster (K)')
plt.ylabel('Silhouette Score')
plt.grid(True)
plt.show()

for k in range(1, 6):
    kmeans = KMeans(n_clusters=k, init='k-means++', random_state=42, n_init=10)
    y_kmeans = kmeans.fit_predict(X)

    plt.figure(figsize=(8, 5))
    if k == 1:
        sns.scatterplot(x=X[:, 0], y=X[:, 1], color='dodgerblue', s=100)
    else:
        sns.scatterplot(x=X[:, 0], y=X[:, 1], hue=y_kmeans, palette='viridis', s=100)

    plt.scatter(kmeans.cluster_centers_[:, 0], kmeans.cluster_centers_[:, 1], s=300, c='red', label='Centroids', marker='X')
    plt.title(f'Hasil Clustering (K={k})')
    plt.xlabel('Transaction Amount')
    plt.ylabel('Account Balance')
    plt.legend()
    plt.show()

"""K-Means for df_arabica_clean

Analisis Segmentasi Kualitas Kopi Arabika Menggunakan Algoritma K-Means Clustering

Kualitas kopi merupakan atribut kompleks yang dipengaruhi oleh berbagai faktor mulai dari genetika, kondisi geografis, hingga metode pemrosesan. Dataset yang digunakan dalam analisis ini bersumber dari basis data Coffee Quality Institute (CQI), sebuah organisasi nirlaba global yang berdedikasi untuk meningkatkan standar kualitas dan nilai kopi melalui program riset, pelatihan, serta sertifikasi. Dataset ini mendokumentasikan hasil evaluasi sensorik terhadap biji kopi jenis Arabika secara sangat komprehensif. Evaluasi sensorik tersebut mencakup berbagai metrik penilaian profesional seperti aroma, rasa (flavor), sisa rasa di mulut (aftertaste), tingkat keasaman (acidity), kekentalan (body), keseimbangan rasa (balance), hingga kebersihan profil rasa (clean cup).

Tujuan utama dari penerapan analisis ini adalah untuk mengidentifikasi pola tersembunyi dan melakukan segmentasi terhadap profil kualitas kopi berdasarkan atribut sensorik utamanya. Dalam implementasi ini, dua variabel esensial yaitu metrik Aroma dan Rasa (Flavor) diekstraksi untuk memetakan kualitas kopi ke dalam beberapa klaster yang memiliki karakteristik serupa. Pendekatan analitik yang digunakan adalah algoritma K-Means Clustering, sebuah metode pembelajaran mesin tanpa pengawasan yang mengelompokkan data berdasarkan kedekatan jarak antara setiap observasi dengan titik pusat (centroid) pada masing-masing klaster. Hasil dari pemetaan ini dapat memberikan wawasan strategis bagi para profesional kopi, petani, hingga pemanggang kopi (roaster) dalam memahami klasifikasi mutu biji kopi, membedakan kopi berstandar komersial dengan kopi spesialti (specialty coffee), serta mendukung pengembangan industri kopi yang lebih berkelanjutan.

Guna memastikan keandalan hasil pembagian klaster, analisis ini menerapkan dua metode evaluasi standar secara berdampingan untuk menentukan jumlah kelompok (K) yang paling representatif:
1. Metode Elbow (WCSS): Metode evaluasi ini menghitung nilai Within-Cluster Sum of Squares, yaitu total jarak kuadrat antara setiap titik data evaluasi kopi dengan pusat klasternya. Algoritma akan mencari titik optimal di mana kurva penurunan variansi mulai melandai secara tajam dan membentuk pola menyerupai sudut siku. Konfigurasi pada titik tersebut mengindikasikan bahwa penambahan klaster profil kopi baru tidak lagi memberikan peningkatan efisiensi pengelompokan yang signifikan.

2. Metode Silhouette: Sebagai pelengkap untuk memvalidasi metode sebelumnya, pendekatan ini mengukur tingkat kualitas batas pemisahan antar klaster kualitas kopi. Metrik Silhouette mengevaluasi seberapa kuat tingkat kohesi suatu data kopi di dalam kelompoknya sendiri sekaligus seberapa jauh separasinya terhadap kelompok kopi dengan profil kualitas yang berbeda. Titik puncak tertinggi pada grafik ini menunjuk pada jumlah klaster yang paling ideal dan terdefinisi dengan sangat baik secara analitik.
"""

import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns
from sklearn.cluster import KMeans
from sklearn.metrics import silhouette_score

df = pd.read_csv('df_arabica_clean.csv')
X = df[['Aroma', 'Flavor']].dropna().values

wcss = []
for i in range(1, 11):
    kmeans = KMeans(n_clusters=i, init='k-means++', random_state=42, n_init=10)
    kmeans.fit(X)
    wcss.append(kmeans.inertia_)

plt.figure(figsize=(10, 5))
plt.plot(range(1, 11), wcss, marker='o')
plt.title('Metode Elbow (WCSS)')
plt.xlabel('Jumlah Cluster (K)')
plt.ylabel('WCSS')
plt.grid(True)
plt.show()

silhouette_scores = []
for i in range(2, 11):
    kmeans = KMeans(n_clusters=i, init='k-means++', random_state=42, n_init=10)
    labels = kmeans.fit_predict(X)
    score = silhouette_score(X, labels)
    silhouette_scores.append(score)

plt.figure(figsize=(10, 5))
plt.plot(range(2, 11), silhouette_scores, marker='o')
plt.title('Metode Silhouette')
plt.xlabel('Jumlah Cluster (K)')
plt.ylabel('Silhouette Score')
plt.grid(True)
plt.show()

for k in range(1, 6):
    kmeans = KMeans(n_clusters=k, init='k-means++', random_state=42, n_init=10)
    y_kmeans = kmeans.fit_predict(X)

    plt.figure(figsize=(8, 5))
    if k == 1:
        sns.scatterplot(x=X[:, 0], y=X[:, 1], color='dodgerblue', s=100)
    else:
        sns.scatterplot(x=X[:, 0], y=X[:, 1], hue=y_kmeans, palette='viridis', s=100)

    plt.scatter(kmeans.cluster_centers_[:, 0], kmeans.cluster_centers_[:, 1], s=300, c='red', label='Centroids', marker='X')
    plt.title(f'Hasil Clustering (K={k})')
    plt.xlabel('Aroma Score')
    plt.ylabel('Flavor Score')
    plt.legend()
    plt.show()